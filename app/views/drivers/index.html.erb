<h1>Drivers</h1>

<% @drivers.each do |driver| %>
  <div class="driver">
    <ul>
      <li>
        <%= driver.ride_id %>
        <%= link_to driver.name, driver_path(driver) %>
        <%= driver.vin %>
        <%= driver.available %>
      </li>
    </ul>
  </div>
<% end %>